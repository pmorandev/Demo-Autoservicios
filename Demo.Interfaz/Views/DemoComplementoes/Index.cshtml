@model IEnumerable<Demo.Interfaz.DemoComplemento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DemoTipoCom.TipoDescripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComplementoPrecio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComplementoDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DemoTipoCom.TipoDescripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComplementoPrecio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComplementoDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
